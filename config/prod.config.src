%% -*- erlang -*-
[
 "config/sys.config",
 {miner,
  [
   %% allow os env var to supply the region, used mainly for miner pro
   %% if value is undefined, miner will try to work out region based on asserted location
   {region_override, '${REGION_OVERRIDE}'}

  ]}
].
